/**
 * Модуль математики
 * @module math
 */
;(function(factory) {
    var registeredInModuleLoader = false;
    if (typeof define === 'function' && define.amd) {
        define(factory);
        registeredInModuleLoader = true;
    }

    if (!registeredInModuleLoader) {
        window.core.math = factory();
    }
})(/** @lends module:math */function() {
    /**
     * Получить наибольший общий делитель двух чисел
     * @param a первое число
     * @param b второе число
     */
    var greatestCommonDivisor = function(a, b) {
        return a != 0 ? greatestCommonDivisor(b % a, a) : b;
    };

    /**
     * Получить случайное число из заданного диапазона
     * @param d диапазон
     */
    var rand = function(d) {
        return Math.floor(Math.random() * (d - 1) + 1);
    };

    return {
        greatestCommonDivisor: greatestCommonDivisor,
        rand: rand
    }
});