// TODO
/**
 * register
 * login
 * logout
 * refreshBalance
 */

/**
 * Модуль пользователя
 * @module user
 */
;(function(factory) {
    var registeredInModuleLoader = false;
    if (typeof define === 'function' && define.amd) {
        define(factory);
        registeredInModuleLoader = true;
    }

    if (!registeredInModuleLoader) {
        window.core.user = factory();
    }
})(/** @lends module:user */function() {


    var register = function() {
        // TODO
    };

    var login = function() {
        // TODO
    };

    var logout = function() {
        // TODO
    };

    var refreshBalance = function() {
        // TODO
    };

    return {
        register: register,
        login: login,
        logout: logout,
        refreshBalance: refreshBalance
    }
});